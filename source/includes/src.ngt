void play_s_wait(string filename,sound@ f=s)
{
if(f.is_active()) f.close();
f.load(filename);
if(!f.is_active()) return;
f.play();
while(f.is_playing())
{
delay(5);
update_game_window();
}
f.close();
}
